# Copy to .env.local for local testing. On Vercel, set these in
# Project Settings > Environment Variables instead — never commit real values.

# Same Walmart API credentials from the CLI tool's .env file
WALMART_CLIENT_ID=
WALMART_CLIENT_SECRET=
WALMART_ENV=production

# Auto-filled by Vercel when you attach a KV (Upstash Redis) database to
# this project — see README.md "Deploying" section. You do not need to
# set these manually if you attach KV through the Vercel dashboard.
KV_REST_API_URL=
KV_REST_API_TOKEN=
